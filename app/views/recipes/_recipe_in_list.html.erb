<%= turbo_frame_tag dom_id(recipe) do %>
  <li class="recipe-li row">
    <p><%= link_to recipe.title, recipe_path(recipe), data: { turbo_frame: "_top" } %></p>
    <p class="visibility-label">
       <% if recipe.is_public %>
        <%= "Publique" %>
      <% else %>
        <%= "Privée" %>
      <% end %>
    </p>
    <%= link_to "Supprimer", recipe_path(recipe), method: :delete, class: "btn btn-danger",
                data: { turbo_confirm: "Supprimer cette recette ?", turbo_method: :delete } %>
  </li>
<% end %>