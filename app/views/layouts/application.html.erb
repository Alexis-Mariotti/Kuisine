<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Kuisine" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <%#= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%# import the webpack application, with all the js and css %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="<%= asset_path('logo_kuisine_small.png') %>" type="image/png">
    <link rel="icon" href="<%= asset_path('logo_kuisine_small.png') %>" type="image/svg+xml">
    <link rel="apple-touch-icon" href="<%= asset_path('logo_kuisine_small.png') %>">

    <link rel="manifest" href="/manifest.json">

    <%= javascript_importmap_tags %>
  </head>
  <%# to permit redirect with turbo %>
  <%= turbo_frame_tag "turbo_redirect" %>

  <body>
    <%= render "layouts/header" %>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
